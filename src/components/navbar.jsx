const Navbar = () => {
  return (
    <div className="navbar bg-primary text-primary-content">
      <button className="btn btn-ghost text-xl">News Api</button>
    </div>
  );
};

export default Navbar;
